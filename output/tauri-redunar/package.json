{
  "name": "redunar-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config vite.config.js",
    "build": "vite build --config vite.config.js --outDir dist",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  }
}
